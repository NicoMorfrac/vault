# Logs

Paperclip is the canonical operational log for tasks, approvals, runs, comments, dependencies, and handoffs.

Do not store credentials, account tokens, billing data, customer lists, raw personal data, or unnecessary account exports here. Create a vault log only when explicitly required and approved, using the report standard.
