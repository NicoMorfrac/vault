# Accounting change proposal — internal, not executed

- Proposal ID / version:
- Assigned issue / human owner:
- Odoo instance / database / company:
- Source timestamps / record IDs:
- Reporting period / accounting basis / currency:
- Current values:
- Proposed values:
- Evidence and reason:
- Expected financial, tax and period effects:
- Dependencies / affected records / uncertainty:
- Human execution steps (description, not a runnable write command):
- Verification query scope / expected results:
- Recovery limitations:
- Required approval for a connector-supported correction: `APPROVE ACCOUNTING CHANGE <Issue-ID> <Version>`
- Exact scope: `ODOO_CHANGE_SCOPE: company=<ID> record=<ID>`
- Current prepared executor: one existing draft invoice/bill, fields `ref`, `invoice_date`, `invoice_date_due` only.
- Approval comment ID / author / timestamp (blank until real approval):
- Named human execution confirmation (blank until received):
- Actual readback / differences:
- Status: CORRECTION_PROPOSED

This template alone authorises nothing. The exact frozen tool plan, later direct human approval, reviewed write connection and verified receipt are required for agent execution. Unsupported operations remain human-executed.
