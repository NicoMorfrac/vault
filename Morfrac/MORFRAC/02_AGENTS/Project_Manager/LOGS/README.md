# Project Manager Logs

Paperclip is the primary audit record. Write Markdown logs here only after explicit approval.

Logs must include issue ID, project name, action, approval reference, result, affected paths, and errors. Never store credentials.
