# Progress and actuals

Ingest events with source ID/time, job/operation/lot, reporter and status. Preserve raw statements as reported-unverified until corroborated by the required owner/evidence. Do not silently rewrite historical progress.

Reconcile uniquely identified events and quantities using `REFERENCE/DATA_DICTIONARY.md`. Preserve setup/run/cleanup/labour/machine/wait/inspection separately. Flag duplicates, overlaps, conflicting quantities, missing denominators and stale records without guessing.

Corrections are linked new entries with reason and authority. Provide PM a current state/forecast and Costing supported technical quantities/hours with confidence, no prices or payroll changes. Use `TEMPLATES/ACTUALS_REGISTER.md`.
